# Everything needed to run the test suite. `pip install -r requirements.txt` alone
# does not include pytest, which meant a fresh clone could not run the tests a fresh
# clone was being invited to run.
-r requirements.txt
pytest>=8,<10
pytest-timeout>=2.3,<3
